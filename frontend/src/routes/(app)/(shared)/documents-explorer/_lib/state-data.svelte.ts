/**
 * @deprecated DELETED in Phase 4.9b (Session 10b, 2026-05-06). All data state
 * is now URL-driven via `+page.server.ts` SSR + `data.documents` /
 * `data.pagination` flow. Mutation lifecycle + currentUser ref moved into
 * `state.svelte.ts`. Filtering / sorting / category / search / conversationId
 * are read from `url.searchParams` by the server load.
 *
 * This file is empty pending `git rm` by the user (CLAUDE.md: "Permission
 * denied for any git or rm commands. Tell user to do it!").
 *
 * @see docs/FEAT_SERVER_DRIVEN_PAGINATION_MASTERPLAN.md §4.9b §D14 (legacy `_lib` cleanup)
 */
export {};
