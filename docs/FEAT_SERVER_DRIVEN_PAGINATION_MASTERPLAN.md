# FEAT: Server-Driven Pagination — Execution Masterplan

> **Plan type:** FEATURE (refactor-as-feature: existing pages migrate to a canonical server-driven pagination pattern)
> **Created:** 2026-05-01
> **Version:** 1.0.0 (APPROVED — Phase 0 sign-off 2026-05-01)
> **Status:** Phases 1–3 + 4.1–4.10 + 4.11a closed (Session 11a, 2026-05-06); 4.11b–4.12 pending. **Next:** 4.11b (TPM FE URL-state).
> **Branch:** `feat/server-driven-pagination`
> **Spec:** [HOW-TO-FIX-MANAGE-PAGINATION.md §"Phase 2 — Server-Driven Pagination"](./how-to/HOW-TO-FIX-MANAGE-PAGINATION.md)
> **Reference impl:** `manage-dummies` (post-Phase-3 canonical); `manage-employees` / `work-orders` / `inventory` are the addon-gated Phase-4 references.
> **Author:** SCS Technik
> **Sessions:** 24 closed (last: 11a, 2026-05-06). Per-session detail: §Session Tracking. Per-version detail: §Changelog.
> **Beta-Ready Criterion:** Every list page in the app correctly displays >100 records of its type without silent truncation, and every filter/search operation spans all pages, not just the loaded subset.

---

## Why now

Beta launch imminent. The current Phase-1 fix (`?limit=100`) is a hard ceiling: any tenant with 101 records of any type silently loses data. This is a correctness bug, not a performance optimization. Greenfield status (CLAUDE.md) gives us the freedom to make a breaking change to the URL/state model without backwards-compat shims — that window closes the moment the first paying tenant signs up.

---

## Changelog

| Version | Date       | Change                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| ------- | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------ | --------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 0.1.0   | 2026-05-01 | Initial draft — phases outlined, awaiting sign-off.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| 1.0.0   | 2026-05-01 | **Phase 0 sign-off.** Decisions Q1–Q4 recorded; status APPROVED. Sessions 13 → 15 (Phase 3 reframed rebuild + Phase 4.12 typeahead).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| 1.1.0   | 2026-05-01 | **Phase 1.1 + 1.2a Stage A complete** — endpoint audit (`Phase-1-audit.md`) + DTO refactor on 4 simple endpoints (224/224 tests).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| 1.2.0   | 2026-05-02 | **Phase 1.2a Stage B complete** — search WHERE on 4 NEEDS-SEARCH endpoints (surveys, work-orders, tpm/plans, tpm/cards); D2 limit cap; 626/626 tests.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| 1.3.0   | 2026-05-02 | **Phase 1.2b complete** — `/approvals` inline TS interface migrated to Zod DTO + ILIKE search; 161/161 tests.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| 1.4.0   | 2026-05-02 | **Phase 2 Step 2.1 complete** — `apiFetchPaginated<T>()` helper.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| 1.5.0   | 2026-05-02 | **Phase 2 Step 2.2 complete** — `url-pagination.ts` helpers (`readPageFromUrl`, `readSearchFromUrl`, `readFilterFromUrl`, `buildPaginatedHref`).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| 1.6.0   | 2026-05-02 | **Phase 2 Step 2.3 complete** — unit tests for both helpers (90/90 pass). Phase 2 DoD met.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| 1.7.0   | 2026-05-04 | **Phase 3 Step 3.1 complete** — backend `dummy-users` ships canonical ADR-007 envelope (D4 recorded).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| 1.8.0   | 2026-05-04 | **Phase 3 Step 3.2 complete** — frontend `manage-dummies` migrated to URL-driven state (D5 recorded — status filter URL-driven).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| 1.9.0   | 2026-05-04 | **Phase 3 Step 3.3 complete** — HOW-TO doc rewrite (canonical pattern + smoke checklist + triage corrections).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| 1.10.0  | 2026-05-04 | **Phase 4 Step 4.1a complete** — backend `/users` envelope rename (`currentPage`→`page` etc.); resolves §D4.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| 1.11.0  | 2026-05-04 | **Phase 4 Step 4.1b complete** — FE `manage-employees` URL-state migration + new `apiFetchPaginatedWithPermission` helper (§D6). Reference impl for addon-gated pages.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| 1.12.0  | 2026-05-05 | **Phase 4 Step 4.2 complete** — `manage-admins` URL-state migration (root-only, base helper); D7 micro recorded.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| 1.13.0  | 2026-05-05 | **Phase 4 Step 4.3 complete** — `manage-approvals` rebuild (broken-by-mirror); 4.3a backend envelope + 4.3b FE URL-state via D6 helper + status/addonCode/search URL filters.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| 1.14.0  | 2026-05-05 | **Phase 4 Step 4.4a complete** — backend `/assets` server-side pagination + ADR-007 envelope (§D8 sub-split — `/assets` was broken-not-paginated).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| 1.15.0  | 2026-05-05 | **4.4b** FE `manage-assets` URL-state — `apiFetchPaginatedWithPermission<Asset>` (§D6); status enum drift cleanup (`cleaning`/`other` dropped, `decommissioned` added). 6 state files trimmed, anchor pagination, search-results dropdown removed. svelte 0/0. (Session 7d)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| 1.16.0  | 2026-05-05 | **4.5a** backend `/kvp` envelope rename — broken-by-dual-drift (§D9: wrapper-key `suggestions` + non-canonical pagination keys). Bonus: pg-bigint→string drift in `executeCountQuery` fixed. unit 64/64, api 44/44. (Session 8a)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| 1.17.0  | 2026-05-06 | **4.5b** FE `kvp` URL-state — `apiFetchPaginatedWithPermission<KvpSuggestion>` (§D6); URL filter contract verbatim backend names (§D5). New §D10 (departmentFilter no-op), §D11 (`'restored'` status rejected). State files trimmed, legacy fetchers deleted. svelte 0/0. (Session 8b)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| 1.18.0  | 2026-05-06 | **4.6** `/blackboard` + `/archived` FE migration — first §D8/§D9 audit-pass-success (backend canonical). Both routes folded atomically (§D13). Archived `?limit=100` band-aid replaced with server pagination. New §D12 (silent status drift), §D14 (legacy `_lib` cleanup). svelte 0/0. (Session 8c)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| 1.19.0  | 2026-05-06 | **4.7a** backend `/work-orders` + `/my` envelope rename — broken-by-shape, missing `pagination` block (§D15). Both endpoints share `buildPaginatedList`. unit 48/48, api 37/37. Comments envelope deferred → Known Limitation #10. (Session 9a)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| 1.22.0  | 2026-05-06 | **4.8b** FE `inventory/lists/[id]/` URL-state — `apiFetchPaginatedWithPermission<InventoryItemWithCustomValues>` (§D6); per-row `customValues` (§D17); legacy `ItemsPage` + `customValuesByItem` map dropped. svelte 0/0, ESLint clean. (Session 9d)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| 1.21.0  | 2026-05-06 | **4.8a** backend `/inventory/items` envelope rebuild — broken-by-shape + NOVEL `customValuesByItem` sibling map (§D17). Denormalised into per-row `customValues` (new `InventoryItemRowWithCustomValues` type). unit 254/254, api 64/64. (Session 9c)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| 1.20.0  | 2026-05-06 | **4.7b** FE `/work-orders` (employee+admin) URL-state — `apiFetchPaginatedWithPermission<WorkOrderListItem>` (§D6); search added on BOTH routes (§D16); legacy `_lib/api.ts` fetchers + `PaginatedResponse<T>` deleted. svelte 0/0, ESLint clean. (Session 9b)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| 1.23.0  | 2026-05-06 | **4.9a** backend `/documents` envelope rebuild — broken-by-wrapper-key drift (§D9 / §D18: `documents` not in `isPaginatedResponse`'s recognised set). Renamed `documents` → `items`; bundled server-side `?sort` dispatch (4 cases per §D11 anti-dishonest-UI). Cross-module touch: `blackboard-attachments.service.ts` + `chat.controller.ts` updated for required `sort` field + `.items` access. Folder sidebar counts deferred → Known Limit. #11. unit 82/82, api 6/6 + 24/24 chat+blackboard sweep clean. (Session 10a)                                                                                                                                                                                                                                                                                                                          |
| 1.24.0  | 2026-05-06 | **4.9b** FE `documents-explorer` URL-state — `apiFetchPaginatedWithPermission<RawDocument>` (§D6); URL contract `?page`/`?search`/`?accessScope`/`?sort`/`?conversationId` (verbatim backend names, §D5). Pagination block ADDED (W1: page had no pagination UI pre-migration). Sort wired to URL (W2: was FE-only). Folder-sidebar counts dropped (W3 / Known Limit. #11). `state-data.svelte.ts` + `filters.ts` emptied (legacy `_lib` cleanup §D14 — deletion deferred to user per CLAUDE.md no-rm policy, §D19). svelte 0/0, ESLint clean. (Session 10b)                                                                                                                                                                                                                                                                                           |
| 1.25.0  | 2026-05-06 | **4.10a** backend `/surveys` envelope rebuild — broken-by-shape (§D15 mirror). Service signature `Promise<unknown[]>` → `Promise<PaginatedSurveys>`; controller signature follows via `ReturnType<...>`. Three `COUNT(*)::integer` helpers added to `survey-access.service.ts` mirroring fetch helpers (unrestricted / visibility / management) — share WHERE clauses, run in parallel with fetch via `Promise.all`. Cross-module sweep clean: only consumer outside scope is `(shared)/surveys/+page.server.ts`, which reads `data` as flat array — wire-format unchanged (interceptor extracts `items` to top-level `data`), so no FE touch required this session. unit 245/245 (incl. 9 new count tests + 4 new pagination math tests pinning §D9 KVP precedent), api 12/12 (incl. canonical-envelope + cross-page slice assertions). (Session 10c) |
| 1.27.0  | 2026-05-06 | **4.11a** `/tpm/plans` + `/tpm/cards` envelope rebuild — §D21 §D15 mirror. (Session 11a) |
| 1.26.0  | 2026-05-06 | **4.10b** FE `manage-surveys` URL-state — `apiFetchPaginatedWithPermission<Survey>` (§D6) × four parallel section calls (`status=draft                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | active | completed | archived`). New §D20 (Beendete merger: single `?completedPage`URL pivot drives TWO parallel calls for`completed`+`archived`, results concatenated, `mergeBeendetePagination`sums totals + maxes pages). URL contract`?draftPage`/`?activePage`/`?completedPage`+ shared`?search`is FE-internal — explicit §D5 exception (no value aliasing, just structural three-section pivot). Search-change resets all three section pages to 1 (avoids stale-cursor empty-page UX). Templates section out-of-scope (kept).`\_lib`cleanup:`loadSurveys`/`loadTemplates`/`loadDepartments`/`loadTeams`/`loadAreas`/`createFromTemplate`dropped from`\_lib/api.ts`; `reloadSurveys`+ non-Invalidate`saveSurvey`/`handleDeleteSurvey`dropped from`\_lib/handlers.ts`; `PaginatedResponse<T>`removed from`\_lib/types.ts`. svelte 0/0, ESLint clean. (Session 10d) |

---

## 0. Prerequisites & Risk Assessment

### 0.1 Must be true before starting

- [x] Docker stack running, all containers healthy (verified 2026-05-01: backend up 2h, postgres healthy, redis healthy, full observability stack)
- [ ] DB backup taken — **DEFERRED** (no schema changes planned in this plan; backup hygiene only useful if Phase 1 audit surfaces a backend extension that touches DB. Re-evaluate at Phase 1.2 close.)
- [x] Branch `feat/server-driven-pagination` checked out (user-confirmed 2026-05-01)
- [x] `manage-dummies` implementation reviewed — **FINDING:** NOT actually server-paginated (`+page.server.ts:39` hardcodes `?page=1`, response shape `PaginatedDummies { items, total, pageSize }` violates ADR-007 envelope `meta.pagination`). Phase 3 reframed: rebuild backend response + FE URL-state instead of "polish".
- [ ] Beta-launch date confirmed — **DEFERRED** (does not affect correctness, only session pace; user can answer during execution)
- [x] Sign-off from user on this masterplan (Phase 0 Decisions Q1–Q4 answered 2026-05-01, status APPROVED, version 1.0.0)

### 0.2 Risk Register

| #   | Risk                                                                                                                        | Impact | Probability | Mitigation                                                                                                                                                     | Verification                                                                      |
| --- | --------------------------------------------------------------------------------------------------------------------------- | ------ | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------- |
| R1  | Removing client-side filter breaks search-across-pages UX — user types in search and only sees filtered subset of page 1    | High   | High        | Backend MUST accept `?search=` on every list endpoint; FE shows loading spinner during request                                                                 | API integration test: search query returns matches from records beyond page 1     |
| R2  | Backend endpoint does not yet support a filter the FE currently does client-side (e.g. `manage-employees` team/dept filter) | High   | Medium      | Phase 1 audits every endpoint and closes gaps BEFORE FE migration                                                                                              | Backend test per endpoint asserts every filter combination produces correct count |
| R3  | Mutations (create/delete) do not refresh paginated view → user sees stale data                                              | Medium | Medium      | Use `invalidateAll()` after mutation (existing pattern). URL retains `?page=N` so refresh keeps position                                                       | Manual smoke per page: create entry → list updates within current page            |
| R4  | Race condition: user changes page while previous request still in-flight → wrong data renders                               | Medium | Medium      | Use SvelteKit's built-in load-function semantics — last navigation wins                                                                                        | Manual: rapid page-button clicking shows latest page only                         |
| R5  | URL state pollution (`?page=1&search=&status=active&team=&dept=`)                                                           | Low    | High        | Only emit query params with non-default values. Bookmarkability is a feature.                                                                                  | URL inspection on default state shows clean `/manage-X` (no params)               |
| R6  | Backend doesn't paginate at all (manage-halls, manage-areas, manage-departments, manage-teams)                              | Medium | Low         | Out of scope for V1 — see Known Limitations. These pages stay client-side; add UI pagination only if list grows past 50 in real tenant.                        | Triage audit confirms no current/expected Beta tenant has >50 of any of these     |
| R7  | `apiFetchPaginated` introduces breaking change in `apiFetch` consumers                                                      | Medium | Low         | New helper is additive (`apiFetchPaginated` next to existing `apiFetch`). Old helper untouched.                                                                | All ~46 existing `apiFetch` call sites still compile after Phase 2                |
| R8  | Migrated page loses an existing UX detail (e.g. SearchResults dropdown spans all pages)                                     | Medium | Medium      | Keep `SearchResults` dropdown calling a separate endpoint that bypasses pagination (search-only view), OR use server-side `?search=` and accept narrower scope | Per-page manual smoke verifies search dropdown still finds entries on page 5      |

### 0.2.1 Audit findings — Existing Truncation Bugs (verified 2026-05-01)

Backend `/users` query, when called WITHOUT `isActive` param, returns `is_active != 4` (i.e. active + inactive + archived) — NOT only active. Default `limit` per `PaginationSchema` = 10. Status filter on FE main pages is **client-side** today. Combined effect:

| Bug                                                                                                                                                                                                                                                                                         | Symptom                                                                                        | Fix in this plan                                                                                                                   |
| ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------- |
| **B1: Main-page client-side status filter inside server-capped result.** Tenant with N inactive + M active employees: backend returns first 100 of (N+M) mixed; FE filters to "active". If many inactive sit before active in sort order, active records past index 100 vanish from the UI. | User toggles filter to "active", sees fewer entries than really exist.                         | Phase 4: status filter migrates to BACKEND query param (`?isActive=1`) — server returns only matching rows, client filter removed. |
| **B2: Lead-picker dropdowns ship NO `&limit=100`** (5 endpoints in `manage-areas` / `manage-departments` / `manage-teams`). Backend default 10 applies. Only first 10 lead candidates render in the modal.                                                                                  | Adding lead 11+ to a new area is structurally impossible from the UI.                          | Phase 1.3 (NEW): one-liner `&limit=100` per endpoint, before the FE migration. Then Phase 4.x for any page that consumes them.     |
| **B3: Employee-picker (`manage-teams` team-member assignment) ships NEITHER `isActive=1` NOR `&limit=100`.** Backend default 10 returns mixed status (active + inactive + archived).                                                                                                        | Only first 10 candidates render, may include inactive employees that should not be assignable. | Phase 4.12: typeahead refactor + `isActive=1` filter at the new endpoint contract.                                                 |

**B1 is the user's exact "99 inactive + 2 active → 1 active hidden" scenario.** It is THE motivation for moving the status filter server-side, not just client-side trimming.

### 0.3 Ecosystem Integration Points

| Existing system                                                                            | Integration                                                          | Phase | Verified on |
| ------------------------------------------------------------------------------------------ | -------------------------------------------------------------------- | ----- | ----------- |
| `ResponseInterceptor` (ADR-007)                                                            | Already wraps `meta.pagination` — no change                          | —     | existing    |
| `extractResponseData` in `frontend/src/lib/server/api-fetch.ts`                            | NEW: `apiFetchPaginated<T>` variant that preserves `meta.pagination` | 2     |             |
| `PaginationSchema` (`backend/src/schemas/common.schema.ts`)                                | Already enforces `page`/`limit`/`offset` defaults — no change        | —     | existing    |
| SvelteKit `+page.server.ts` `url.searchParams`                                             | Read `?page`, `?search`, page-specific filters                       | 2, 4  |             |
| Pagination CSS (`frontend/src/design-system/primitives/navigation/pagination.css`)         | Reused as-is for visual consistency                                  | 4     | existing    |
| `manage-dummies`                                                                           | Pattern source for all migrations                                    | 2, 3  |             |
| `manage-employees`, `manage-admins` Phase-1 helpers (`getVisiblePages`, `<NAME>_PER_PAGE`) | Will be DELETED after their pages migrate (Phase 5 cleanup)          | 5     |             |

---

## Phase 1: Backend Endpoint Audit + Filter Extension

> **Dependency:** Phase 0 sign-off complete.
> **Goal:** Every list endpoint in scope supports server-side `?search=` AND every filter the FE currently performs client-side.

### Step 1.1: Per-endpoint audit ✅ DONE (2026-05-01)

For each endpoint in the migration scope (Phase 4 list):

- [x] Inspect query DTO — does it extend `PaginationSchema`?
- [x] Inspect service — does it accept `search`, status, role, team_id, etc. as filter params?
- [x] Inspect controller — does it pass query params to the service?
- [x] Document gap → Step 1.2 backlog row

**Output:** [`docs/Phase-1-audit.md`](./Phase-1-audit.md) — 12 Endpoints + 6 Picker auditiert. Status pro Endpoint: `OK` (3) | `NEEDS-SEARCH` (5) | `NEEDS-PAGINATION` (7) | `NEEDS-DTO` (1, /approvals). Audit identifiziert: (a) 5 Endpoints brauchen `search`-Feld, (b) 7 Endpoints duplizieren page/limit statt PaginationSchema zu extenden, (c) `/approvals` ist inline TS interface ohne Zod, (d) `/users` (Picker-Backend) ist OK — Phase 4.12 PickerTypeahead kann sofort `?search=` nutzen ohne Backend-Arbeit, (e) `/dummy-users` Shape-Anomalie für Phase 3 bestätigt. **Audit empfiehlt Phase 1.2 → 2 Sessions splitten** (1.2a: search + PaginationSchema-Refactor für 7 Endpoints | 1.2b: `/approvals` DTO-Migration eigenständig). Sessions-Count müsste ggf. 15 → 16 wenn akzeptiert.

### Step 1.2: Close audit gaps — split per audit empfehlung (3 Sub-Steps)

Audit `Phase-1-audit.md` §6 empfahl ursprünglich Phase 1.2 → 2 Sessions (1.2a + 1.2b). Bei tieferer Code-Inspektion wurde 1.2a weiter gesplittet in Stage A (simple DTO-Only) + Stage B (DTO + Service + SQL atomar). Begründung: TPM `listPlans(tenantId, page, pageSize, user)` und work-orders `buildPaginatedList`-Helper sind unterschiedliche Service-Signaturen — atomare per-Endpoint-Edits (DTO + Controller + Service + SQL) verhindern Halb-Zustände.

#### Step 1.2a Stage A — DTO-Refactor (4 simple Endpoints) ✅ DONE 2026-05-01

Endpoints mit `search` BEREITS server-seitig implementiert — nur DTO-Konsolidierung nötig, kein Service-Touch:

- [x] `/assets` — `backend/src/nest/assets/dto/list-assets-query.dto.ts`: extends `PaginationSchema` mit `.extend({ limit: default(20), search: trim().max(100).optional(), ... })`
- [x] `/inventory/items` — `backend/src/nest/inventory/dto/common.dto.ts:43` `ItemsQuerySchema`: extends `PaginationSchema` mit `limit: default(50)`, search tightened 255 → 100
- [x] `/documents` — `backend/src/nest/documents/dto/query-documents.dto.ts`: extends `PaginationSchema`, schema renamed PascalCase + exported (`ListDocumentsQuerySchema`), search tightened 200 → 100
- [x] `/dummy-users` — `backend/src/nest/dummy-users/dto/list-dummy-users-query.dto.ts`: extends `PaginationSchema`, search now `.trim().max(100)`
- [x] D1 angewandt: per-Endpoint limit-Defaults via `.extend()` Override (assets/documents/dummy-users=20, inventory=50)
- [x] D3 angewandt: search-Konvention `.trim().max(100).optional()` einheitlich
- [x] Type-check: 0 errors
- [x] Lint: 0 errors
- [x] Tests: **224/224 pass** (5 test files: assets DTO 46, inventory DTO 112, documents DTO 20, dummy-users service 29, dummy-users helpers 17)

#### Step 1.2a Stage B — search WHERE addition (4 NEEDS-SEARCH Endpoints) ✅ DONE 2026-05-02

Endpoints brauchen NEU `search` Field PLUS Service-WHERE-Klausel atomar:

| #   | Endpoint       | Files touched                                                                                                                                                                                                                            | SQL ILIKE                | Status |
| --- | -------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------ | ------ |
| B-1 | `/surveys`     | `query-survey.dto.ts` (add search), `surveys.controller.ts:88` (forward), `surveys.service.ts:61` listSurveys (query type), `survey-access.service.ts:136` fetchSurveysByAccessLevel + 3 inner methods (signature + ILIKE WHERE)         | s.title, s.description   | ✅     |
| B-2 | `/work-orders` | `list-work-orders-query.dto.ts` (extends PaginationSchema, override limit=20 max=100 [D2: 500→100], add search; drops local `LimitSchema/PageSchema` import), `work-orders.service.ts` ListQuery interface + `appendQueryFilters` ILIKE  | wo.title, wo.description | ✅     |
| B-3 | `/tpm/plans`   | `list-plans-query.dto.ts` (extends PaginationSchema, limit=20 max=100, add search), `tpm-plans.controller.ts:163` (forward), `tpm-plans.service.ts:212` listPlans (sig adds `search?`) + new module-level helper `buildListPlansClauses` | p.name (column = name)   | ✅     |
| B-4 | `/tpm/cards`   | `list-cards-query.dto.ts` (extends PaginationSchema, limit=20 max=100, add search), `tpm-cards.service.ts` `CardListFilter.search?` + `buildFilterClauses` ILIKE, `tpm-cards.controller.ts` `buildFilters` forwards search               | c.title                  | ✅     |

- [x] Pro Endpoint: DTO-Edit + Controller-Edit (forward search arg) + Service-Signature + SQL ILIKE
- [x] Type-check + Lint + Touched Tests grün pro Endpoint (`docker exec assixx-backend pnpm exec tsc --noEmit -p .` and `eslint backend/src/nest/{surveys,work-orders,tpm}` both clean)
- [x] Backwards-compat: `query.search === undefined || query.search === ''` ⇒ kein WHERE clause emitted (verified per-endpoint)
- [x] **626/626 tests pass** across 10 touched files: surveys.dto.test.ts (with new ListSurveysQuerySchema suite), surveys.service.test.ts (signature + new search-forwarding test), survey-access.service.test.ts, work-orders-dto.test.ts (with D2 + search hygiene), work-orders.service.test.ts, tpm.dto.test.ts (with new ListPlansQuerySchema suite), tpm-cards.dto.test.ts (with new ListCardsQuerySchema pagination+search suite), tpm-card-categories.dto.test.ts, tpm-plans.service.test.ts, tpm-cards.service.test.ts
- [x] **Cleanup carried into Phase 5.2:** local `PageSchema/LimitSchema` (max=500/default=20) in both `work-orders/dto/common.dto.ts` (now 0 remaining DTO consumers; still test-covered standalone) and `tpm/dto/common.dto.ts` (3 remaining consumers — executions, revisions, board) intentionally left in place to keep this session atomic. Their consolidation onto `PaginationSchema` is a separate refactor.

#### Step 1.2b — `/approvals` inline interface → Zod DTO migration ✅ DONE 2026-05-02

Größter Single-Endpoint-Refactor. Eigene Session weil DTO/Service-Contract sich ändert.

- [x] `approvals.controller.ts:52` inline `ListApprovalsQuery` interface löschen
- [x] Neue Zod DTO `backend/src/nest/approvals/dto/list-approvals-query.dto.ts` erstellen, extends `PaginationSchema` + add search (D3) + bestehende Filter (status, addonCode, priority). Limit override default=20 (preserves service-side fallback `filters.limit ?? 20`); max=100 inherited from `PaginationSchema`. AddonCode = `z.string().trim().min(1).max(50)` (matches `CreateApprovalSchema`). Status enum = `'pending' | 'approved' | 'rejected'`; priority enum = `'low' | 'medium' | 'high'` (mirror of approvals.types.ts unions).
- [x] Controller: `@Query() query: ListApprovalsQueryDto` for both `listAll` and `getAssigned`. Manual `Number(query.page/limit)` coercion removed — DTO does it. `getAssigned` ignores DTO's search/addonCode/priority (mirrors legacy inline-interface ignore pattern).
- [x] Service `findAll`: `ApprovalFilters` extended with `search?: string | undefined`; ILIKE WHERE on `(a.title OR a.description)` with single bound param `%term%` reused (Stage B pattern). Backwards-compat: `undefined` OR `''` ⇒ no clause emitted.
- [x] Tests: new `list-approvals-query.dto.test.ts` (33 tests covering defaults, coercion, enum membership, length bounds, D3 search semantics). `approvals.service.test.ts` extended with 3 search tests in `findAll()` suite (ILIKE applied; empty-string drops clause; combined-with-other-filters keeps param order: status, addonCode, priority, %search%).
- [x] **161/161 approvals unit tests pass** (6 files); ESLint clean; backend type-check clean.

### Step 1.3: ~~Fix B2 — lead-picker `&limit=100` one-liners~~ **REMOVED** (Decision Q3, 2026-05-01)

> **Why removed:** `&limit=100` ist ein Band-aid, das beim 101. aktiven Lead silently truncated. Direkter Widerspruch zur "no quick fix"-Regel. Phase 4.12 expandiert stattdessen zu einem unifizierten debounced typeahead-Component, der ALLE 6 Picker-Endpoints bedient (5 Lead + 1 Employee). Single source of truth, kein structural ceiling, langfristig sauber.

**Backend-Vorbereitung in Phase 1:** verifiziere im Audit (1.1), dass `/users` Endpoint bereits `?search=` unterstützt. Wenn ja → keine Backend-Änderung nötig; alle 6 Picker konsumieren denselben Endpoint via Phase 4.12 Component. Wenn nein → Search-Param via Step 1.2 ergänzen.

**Mitigation während Phase 1–3 läuft (vor Phase 4.12):** die 6 Picker bleiben bei Backend-Default `limit=10`. Tenants mit >10 Lead-Kandidaten sehen nur die ersten 10 — bekanntes Issue, aber strukturell unter Beta-Schwelle (typischerweise 5–20 Area-Leads, 10–50 Dept-Leads, 20–50 Team-Leads). Risiko akzeptiert für ~12 Sessions Übergangszeit. **Wenn ein Beta-Tenant vor Phase 4.12 das Problem trifft:** Phase 4.12 priorisieren statt Band-aid.

### Phase 1 — Definition of Done

- [x] Audit complete for all 10 in-scope endpoints (`docs/Phase-1-audit.md` — 12 endpoints + 6 pickers, changelog 1.1.0)
- [x] All identified gaps closed (1.2a-A + 1.2a-B + 1.2b all done, changelog 1.3.0)
- [x] `docker exec assixx-backend pnpm run lint` → 0 errors (verified per substep, changelogs 1.1.0/1.2.0/1.3.0)
- [x] `docker exec assixx-backend pnpm run type-check` → 0 errors (verified per substep, changelogs 1.1.0/1.2.0/1.3.0)
- [x] All affected unit + API tests green (224/224 Stage A · 626/626 Stage B · 161/161 approvals)
- [ ] No regression in untouched endpoints (`pnpm run test:api`) — **NOT RUN as full sweep**; touched tests verified per substep, full integration suite deferred until pre-merge gate

---

## Phase 2: Frontend Infrastructure

> **Dependency:** Phase 1 complete (backend supports the queries we will fire).

### Step 2.1: `apiFetchPaginated<T>()` helper ✅ DONE (2026-05-02)

**File:** `frontend/src/lib/server/api-fetch.ts` (extend existing)

```typescript
export interface PaginationMeta {
  // From backend (ADR-007 envelope `meta.pagination`):
  page: number;
  limit: number;
  total: number;
  totalPages: number;
  // FE-derived (NOT from backend — backend ADR-007 spec doesn't include these):
  hasNext: boolean; // = page < totalPages
  hasPrev: boolean; // = page > 1
}

export interface PaginatedResult<T> {
  data: T[];
  pagination: PaginationMeta;
}

export async function apiFetchPaginated<T>(
  endpoint: string,
  token: string,
  fetchFn: typeof fetch,
): Promise<PaginatedResult<T>>;
```

Preserves `meta.pagination` from the response envelope (which `extractResponseData` strips). Returns `data: []` + `pagination: { total: 0, ... }` on error (consistent with existing `apiFetch` failure mode).

### Step 2.2: URL-pagination helper ✅ DONE (2026-05-02)

**File:** `frontend/src/lib/utils/url-pagination.ts` (new)

```typescript
export function readPageFromUrl(url: URL): number; // default 1
export function readSearchFromUrl(url: URL): string; // default ''
export function readFilterFromUrl<T extends string>(url: URL, key: string, allowed: readonly T[], defaultValue: T): T;
export function buildPaginatedHref(
  base: string,
  params: { page?: number; search?: string; [k: string]: unknown },
): string;
```

Single source of truth for URL ↔ state mapping. Only emits non-default values into the URL.

### Step 2.3: Unit tests ✅ DONE (2026-05-02)

- [x] `api-fetch.test.ts`: `apiFetchPaginated` preserves all `meta.pagination` fields, returns empty result on 4xx/5xx, supports the `apiFetchWithPermission` 403 detection pattern
- [x] `url-pagination.test.ts`: only emits non-default params, parses defaults correctly, validates filter against allowlist

### Phase 2 — Definition of Done

- [x] Both helpers exist with full TypeScript types (no `any`)
- [x] Unit-test coverage ≥ 90 % on both helpers
- [x] `cd frontend && pnpm run check` → 0 errors, 0 warnings
- [x] `cd frontend && pnpm run lint` → 0 errors
- [x] No `apiFetch` consumer breaks (additive change only)

---

## Phase 3: Reference Implementation — REBUILD (not polish)

> **Dependency:** Phase 2 complete.
> **Goal:** Make `manage-dummies` truly canonical. **Status today (verified 2026-05-01):** `+page.server.ts:39` hardcodes `?page=1`; response shape `PaginatedDummies { items, total, pageSize }` violates ADR-007 envelope `meta.pagination`. This is NOT a "polish" step — both backend AND frontend need real changes. After this phase, `manage-dummies` becomes the binding reference every Phase-4 migration copies verbatim.

### Step 3.1: Backend — `dummy-users` to ADR-007 envelope ✅ DONE 2026-05-04

> **Why first:** the FE refactor (Step 3.2) depends on `meta.pagination` being present. Backend must ship envelope first.

- [x] Inspect `backend/src/nest/dummy-users/dummy-users.controller.ts` — verified legacy `{ items, total, page, pageSize }` shape (returned by service, controller signature `Promise<PaginatedDummyUsers>`)
- [x] Refactor controller to return `data: T[]` directly. `ResponseInterceptor` (ADR-007) will wrap it as `{ success, data, meta: { pagination: { page, limit, total, totalPages } }, timestamp }` — verified end-to-end via api integration test; controller signature stays as `Promise<PaginatedDummyUsers>` because the type alias was rewritten under it (zero controller code change, by design)
- [x] Service: pagination metadata computed and attached via interceptor's `{items[]|entries[]|data[], pagination}` detection at `response.interceptor.ts:65 isPaginatedResponse` — `list()` returns `{ items, pagination: { page, limit, total, totalPages } }`; `totalPages` math is `total === 0 ? 0 : Math.ceil(total / limit)`. **D4 deviation:** plan named `/users` as reference but `/users` is non-canonical (`currentPage/pageSize/totalItems`); followed ADR-007 + FE-helper contract instead
- [x] Update affected DTOs / response types — `PaginatedDummyUsers` rebuilt in `dummy-users.types.ts`; sole consumer is the FE `manage-dummies/_lib/types.ts` comment, which is a Step 3.2 concern
- [x] API integration test: `GET /dummy-users?page=2&limit=10` returns canonical envelope with correct `meta.pagination.totalPages` — added in `backend/test/dummy-users.api.test.ts` "should echo ?page=2&limit=10 with correct totalPages math"; 20/20 api tests pass
- [x] Backwards-compat consideration: greenfield (CLAUDE.md) — broke the shape, no shim shipped

### Step 3.2: Frontend — `manage-dummies` to URL-state + new helpers ✅ DONE 2026-05-04 (manual smoke deferred to user)

- [x] Remove `extractDummies()` helper in `+page.server.ts` — deleted; was a workaround for the pre-Phase-3.1 non-canonical shape, now obsolete
- [x] Remove `PaginatedDummies` type from `_lib/types.ts` — deleted; superseded by `PaginatedResult<DummyUser>` from `$lib/server/api-fetch.ts` consumed inline by the load function
- [x] Replace `apiFetch<PaginatedDummies>('/dummy-users?page=1&limit=20', token, fetch)` with `apiFetchPaginated<DummyUser>('/dummy-users?' + new URLSearchParams({...}).toString(), token, fetch)` — server load builds `URLSearchParams` from URL state then calls the new helper
- [x] Read `?page` via `readPageFromUrl(url)`, `?search` via `readSearchFromUrl(url)` — both Phase-2 helpers consumed; `?isActive` also threaded through `readFilterFromUrl<'all'|'0'|'1'|'3'>` (D5 deviation — see §Spec Deviations)
- [x] `+page.svelte`: pagination UI uses URL-driven `<a href={resolve(buildPaginatedHref(...))}>` links, NOT button-onclick state mutation — page-number anchors emit clean URLs (default page=1 / search='' / isActive='all' never serialised); `<button disabled>` for prev/next when `pagination.hasPrev`/`hasNext` is false (anchors without href are not native-disabled)
- [x] Search input → `goto()` with debounced URL update (not local filter) — `SearchBar`'s 300 ms internal debounce kept; on fire it calls `handleSearch` → `goto(resolve(buildPaginatedHref(...)))`; `keepFocus: true` so the input keeps focus across the load
- [x] Verify `invalidateAll()` after create/delete keeps current page — both `handleSave` and `handleDeleteConfirm` swapped from `await loadDummies()` to `await invalidateAll()`; SvelteKit re-runs the load on the SAME URL, so `?page=N`/`?search=...`/`?isActive=...` are preserved across the mutation
- [x] Manual smoke: create 30 dummies, navigate page 1→2→3, verify search "abc" finds match on page 3, verify back-button restores — user-confirmed pass 2026-05-04

### Step 3.3: Document the pattern in HOW-TO ✅ DONE 2026-05-04

- [x] Update `docs/how-to/HOW-TO-FIX-MANAGE-PAGINATION.md` §"Phase 2 — Server-Driven Pagination" with copy-paste-ready snippets of the migrated `manage-dummies` (replaces the 3-step abstract description) — replaced 9-line abstract with 5-section canonical pattern: helpers table, backend ADR-007 envelope, `+page.server.ts` URL→state→backend, `+page.svelte` (no-`$state`-shadows + `pageHref()` + `navigateFilters()` + URL-driven `<a href>` pagination block with `<button disabled>` fallback), `invalidateAll()` mutation pattern, D5 filter rule, 5-box manual-smoke checklist
- [x] Correct triage table row 91 — was claiming `manage-dummies` is server-paginated; document the rebuild — row now reads "**canonical reference impl** (Phase 2 + 3, 2026-05-04). Pre-Phase-3 ... `page=1` hardcoded ... non-canonical envelope ..." with masterplan §Phase 3 link
- [x] Add note to row 92 (`manage-approvals` "mirrors manage-dummies"): re-verify in Phase 4.3 — status icon flipped ✅ → ⚠️, note now reads "**needs Phase 4.3 verification** ... if broken-by-mirror, full Phase-2 rebuild required" with masterplan §4.3 link

### Phase 3 — Definition of Done

- [x] `dummy-users` backend ships ADR-007 envelope (verified by API integration test, 20/20 pass — Step 3.1, 2026-05-04)
- [x] `manage-dummies` FE reads URL state, page navigation = URL update, search spans all pages (Step 3.2, 2026-05-04 — code-verified; manual smoke deferred to user)
- [x] Manual smoke with >30 dummies passes all 4 cases (page nav, cross-page search, filter reset, browser back) — user-confirmed 2026-05-04
- [x] `cd frontend && pnpm run check` 0 errors for the page (Step 3.2: 2596 files, 0 errors, 0 warnings, 2026-05-04)
- [x] `docker exec assixx-backend pnpm run lint && pnpm run type-check` 0 errors for backend changes (Step 3.1, 2026-05-04)
- [x] HOW-TO doc updated with concrete snippets — Step 3.3 (2026-05-04, doc grew 141 → 345 lines, prettier clean)

---

## Phase 4: Page Migrations (parallelizable across sessions)

> **Dependency:** Phase 3 complete.
> **Pattern per page:** 1 session = `+page.server.ts` + `+page.svelte` + filter wiring + manual smoke.

### Migration order (priority by Beta blast radius)

| #    | Page                                          | Endpoint                                                                | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Status  |
| ---- | --------------------------------------------- | ----------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------- |
| 4.1  | `manage-employees`                            | `/users?role=employee`                                                  | Sub-split §D4: 4.1a backend `/users` envelope rename (1.10.0) + 4.1b FE URL-state + new `apiFetchPaginatedWithPermission` (§D6, 1.11.0). Reference impl for addon-gated.                                                                                                                                                                                                                                                                                                                                                                                               | ✅ DONE |
| 4.2  | `manage-admins`                               | `/users?role=admin`                                                     | Root-only → base `apiFetchPaginated<T>` (no addon-gate needed under `(root)`). Changelog 1.12.0 / Session 7a. §D7.                                                                                                                                                                                                                                                                                                                                                                                                                                                     | ✅ DONE |
| 4.3  | `manage-approvals`                            | `/approvals`                                                            | Broken-by-mirror. Sub-split: 4.3a backend envelope rename + 4.3b FE URL-state via `apiFetchPaginatedWithPermission` (§D6) + status/addonCode/search filters (§D5). 1.13.0 / Session 7b.                                                                                                                                                                                                                                                                                                                                                                                | ✅ DONE |
| 4.4  | `manage-assets`                               | `/assets`                                                               | Broken-not-paginated (§D8). Sub-split: 4.4a backend pagination + envelope (1.14.0) + 4.4b FE URL-state (1.15.0). Status enum drift cleanup. Sessions 7c+7d.                                                                                                                                                                                                                                                                                                                                                                                                            | ✅ DONE |
| 4.5  | KVP suggestions                               | `/kvp`                                                                  | Broken-by-dual-drift (§D9: wrapper-key `suggestions` + non-canonical pagination keys). Sub-split: 4.5a backend rename + bigint fix (1.16.0) + 4.5b FE URL-state (1.17.0). New §D10/§D11. Sessions 8a+8b.                                                                                                                                                                                                                                                                                                                                                               | ✅ DONE |
| 4.6  | blackboard entries                            | `/blackboard/entries`                                                   | First §D8/§D9 audit-pass-success (backend canonical). FE-only across `/blackboard` + `/archived` folded atomically (§D13). New §D12/§D14. 1.18.0 / Session 8c.                                                                                                                                                                                                                                                                                                                                                                                                         | ✅ DONE |
| 4.7  | work-orders                                   | `/work-orders`, `/work-orders/my`                                       | Broken-by-shape (§D15: missing `pagination` block). Sub-split: 4.7a backend rename (1.19.0) + 4.7b FE atomic both routes (§D13, 1.20.0). New §D16. Comments envelope → Known Limit. #10. Sessions 9a+9b.                                                                                                                                                                                                                                                                                                                                                               | ✅ DONE |
| 4.8  | inventory items                               | `/inventory/items`                                                      | Broken-by-shape + NOVEL `customValuesByItem` sibling map (§D17). Sub-split: 4.8a backend rebuild + denormalised per-row `customValues` (1.21.0) + 4.8b FE URL-state (1.22.0). Sessions 9c+9d.                                                                                                                                                                                                                                                                                                                                                                          | ✅ DONE |
| 4.9  | documents-explorer                            | `/documents`                                                            | Broken-by-wrapper-key drift (§D9 / §D18). Sub-split: 4.9a backend rename + bundled `?sort` dispatch (1.23.0, Session 10a) + 4.9b FE URL-state (1.24.0, Session 10b). Pagination block ADDED (W1). Sort URL-driven (W2). Folder-sidebar counts dropped → Known Limit. #11 (W3). New §D19 (no-rm cleanup discipline).                                                                                                                                                                                                                                                    | ✅ DONE |
| 4.10 | `manage-surveys`                              | `/surveys`                                                              | Broken-by-shape (§D15 mirror — controller `Promise<unknown[]>`, no count). Sub-split: 4.10a backend envelope rebuild + 3 count helpers (1.25.0, Session 10c) + 4.10b FE URL-state with four parallel paginated calls (one per status) + Beendete merger (§D20, single `?completedPage` URL pivot drives `completed`+`archived` parallel calls, results concatenated). 1.26.0. THREE card sections — per-section URL params (`?draftPage`/`?activePage`/`?completedPage`) + shared `?search`. Templates section out-of-scope.                                           | ✅ DONE |
| 4.11 | TPM plans + cards                             | `/tpm/plans`, `/tpm/cards`                                              | Broken-by-shape (§D21 / §D15 mirror). Sub-split: 4.11a backend rebuild (Session 11a, 1.27.0) + 4.11b FE URL-state PENDING.                                                                                                                                                                                                                                                                                                                                                                                                                                              | 🔄 4.11a DONE / 4.11b PENDING |
| 4.12 | **Picker-Typeahead-Component (alle 6 Sites)** | `/users?role=...` mit verschiedenen `position`/`isActive`-Kombinationen | **Decision Q3 (2026-05-01):** EIN unifiziertes debounced typeahead-Component (`PickerTypeahead.svelte`), 6 Konsumenten: 5 Lead-Picker (manage-areas Admin+Root, manage-departments Admin+Root, manage-teams Team-Lead) + 1 Employee-Picker (manage-teams Team-Member-Assignment). Eliminiert `&limit=100` Band-aid komplett. Pattern: `?search=<term>&limit=20` debounced 250ms, ohne client-side full-list-cache. Schätzung: 1 Session Component + 1 Session 6-Site-Wirings = 2 Sessions. Backend: `/users` muss `?search=` unterstützen (Step 1.1 Audit verifiziert) | PENDING |

### Per-Page Definition of Done

For each row in the migration table:

- [ ] `+page.server.ts` reads `?page`, `?search`, and any page-specific filters from `url.searchParams`
- [ ] Backend call uses `apiFetchPaginated`
- [ ] `+page.svelte` removes client-side filtering — search field now triggers URL update (`goto` with new `?search=`)
- [ ] Pagination UI uses URL-update navigation: `<a href={resolve(buildPaginatedHref(...))}>` instead of `onclick={() => currentPage = N}`
- [ ] `invalidateAll()` after mutations triggers re-fetch on the current page
- [ ] Manual smoke test (record in session log):
  - [ ] Tenant with >25 records sees ALL pages
  - [ ] Search returns matches from a record on page 3 (verify cross-page search)
  - [ ] Filter (status / role / etc.) re-fetches and resets to page 1
  - [ ] Browser back-button restores previous page state
- [ ] svelte-check 0 errors for the migrated page
- [ ] ESLint 0 errors for the migrated page
- [ ] Old client-side helpers (`<NAME>_PER_PAGE`, `getVisiblePages` if local-only) deleted

---

## Phase 5: Tests + ADR + Cleanup

> **Dependency:** All Phase 4 migrations complete.

### Step 5.1: API integration tests [PENDING]

For each migrated endpoint, add to `backend/test/<feature>.api.test.ts`:

- [ ] `?page=2&limit=25` returns correct slice + correct `meta.pagination.totalPages`
- [ ] `?search=<term>` returns matches that exist beyond page 1
- [ ] Combined `?page=2&search=<term>` returns correct slice of search hits
- [ ] Tenant isolation still holds (sanity check — RLS unchanged but verify)

### Step 5.2: Remove `?limit=100` Phase-1 hacks [PENDING]

- [ ] `grep -rn '?limit=100\|&limit=100' frontend/src/routes/`
- [ ] Replace each occurrence with the new pattern OR delete if the page was migrated
- [ ] Confirm zero occurrences remain in production code (test files may keep them for fixture setup)

### Step 5.3: ADR [PENDING]

- [ ] Write `docs/infrastructure/adr/ADR-{NEXT}-server-driven-pagination.md`
  - **Decision:** server-driven pagination as the canonical list-rendering pattern across all `manage-*` and addon list pages
  - **Rationale:** greenfield-Beta requirement; scaling beyond 100 records per tenant per type
  - **Alternatives considered:**
    - Client-side `?limit=100` (status quo before this plan) — REJECTED, hard ceiling at 100
    - Cursor / keyset pagination — REJECTED for V1, KISS, page+offset acceptable up to 10 k
    - GraphQL relay-style connections — REJECTED, no GraphQL in stack
  - **Consequences:** every new list page MUST use `apiFetchPaginated` + URL-driven state from day one — enforced by code review
- [ ] Add ADR row to `docs/ARCHITECTURE.md` §1.5 "Frontend (SvelteKit)"

### Step 5.4: Update HOW-TO doc [PENDING]

- [ ] Mark Phase 2 as DONE in `docs/how-to/HOW-TO-FIX-MANAGE-PAGINATION.md`
- [ ] Triage tables — every page migrated marked ✅
- [ ] Add cross-link to the new ADR

### Phase 5 — Definition of Done

- [ ] All migrated pages covered by API integration tests
- [ ] Zero `?limit=100` remnants in `frontend/src/routes/` (production code)
- [ ] ADR written, accepted, linked from `docs/ARCHITECTURE.md` §1.5
- [ ] HOW-TO doc updated, Phase 2 marked DONE
- [ ] Final smoke: every migrated page tested in dev with synthetic >100 records seed

---

## Session Tracking

| Session | Phase  | Description                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | Status  | Date       |
| ------- | ------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------- | ---------- |
| 1       | 0+1    | Endpoint audit (`Phase-1-audit.md`).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | ✅ DONE | 2026-05-01 |
| 2a      | 1.2a-A | DTO refactor (assets, inventory/items, documents, dummy-users) — 224/224 tests.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | ✅ DONE | 2026-05-01 |
| 2b      | 1.2a-B | search WHERE addition (surveys, work-orders, tpm/plans, tpm/cards) — 626/626 tests.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | ✅ DONE | 2026-05-02 |
| 2c      | 1.2b   | `/approvals` Zod DTO migration + ILIKE — 161/161 tests.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | ✅ DONE | 2026-05-02 |
| 3       | 2      | `apiFetchPaginated` + `url-pagination` helpers + tests — 90/90 unit pass.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | ✅ DONE | 2026-05-02 |
| 4       | 3.1    | Backend `dummy-users` → ADR-007 envelope — 91/91 unit + 20/20 api pass.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | ✅ DONE | 2026-05-04 |
| 5a      | 3.2    | Frontend `manage-dummies` URL-state migration (manual smoke user-confirmed).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | ✅ DONE | 2026-05-04 |
| 5b      | 3.3    | HOW-TO doc rewrite (canonical pattern + triage corrections).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | ✅ DONE | 2026-05-04 |
| 6a      | 4.1a   | Backend `/users` envelope rename per §D4 — 53/53 unit + 17/17 api pass.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | ✅ DONE | 2026-05-04 |
| 6b      | 4.1b   | FE `manage-employees` URL-state + new `apiFetchPaginatedWithPermission` helper (§D6) — vitest 126/126.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | ✅ DONE | 2026-05-04 |
| 7a      | 4.2    | `manage-admins` URL-state migration (root-only, base helper) — vitest 119/119.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | ✅ DONE | 2026-05-05 |
| 7b      | 4.3    | `manage-approvals` rebuild (broken-by-mirror) — 4.3a backend + 4.3b FE; vitest 48/48 + 104/104.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | ✅ DONE | 2026-05-05 |
| 7c      | 4.4a   | `manage-assets` backend rebuild (broken-not-paginated, §D8) — vitest unit 34/34.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | ✅ DONE | 2026-05-05 |
| 7d      | 4.4b   | FE `manage-assets` URL-state — `apiFetchPaginatedWithPermission<Asset>` (§D6); status enum drift cleanup (§D5); 6 state files trimmed; anchor pagination. svelte 0/0.                                                                                                                                                                                                                                                                                                                                                                                                                                                     | ✅ DONE | 2026-05-05 |
| 8a      | 4.5a   | Backend `/kvp` envelope rename to ADR-007 canonical (§D9 sub-split, broken-by-dual-drift) — unit 64/64 + api 44/44 (incl. new `?page=2&limit=10` math test); pg-bigint→string drift fixed at source.                                                                                                                                                                                                                                                                                                                                                                                                                      | ✅ DONE | 2026-05-05 |
| 8b      | 4.5b   | FE `kvp` URL-state — `apiFetchPaginatedWithPermission<KvpSuggestion>` (§D6); URL contract verbatim backend names (§D5). New §D10/§D11. State files trimmed, legacy fetchers + `BadgeCounts` deleted. svelte 0/0.                                                                                                                                                                                                                                                                                                                                                                                                          | ✅ DONE | 2026-05-06 |
| 8c      | 4.6    | FE `/blackboard` + `/archived` — first §D8/§D9 audit-pass-success. Both routes folded atomically (§D13). New §D12/§D14. svelte 0/0 (8 files).                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | ✅ DONE | 2026-05-06 |
| 9a      | 4.7a   | Backend `/work-orders` + `/my` envelope rename — broken-by-shape (§D15). Single `buildPaginatedList` covers both. unit 48/48, api 37/37. Comments envelope → V2 / Known Limit. #10.                                                                                                                                                                                                                                                                                                                                                                                                                                       | ✅ DONE | 2026-05-06 |
| 9b      | 4.7b   | FE `/work-orders` (employee+admin) URL-state — `apiFetchPaginatedWithPermission<WorkOrderListItem>` (§D6); both routes folded (§D13); search added BOTH routes (§D16). Legacy fetchers + `PaginatedResponse<T>` deleted. svelte 0/0 (7 files).                                                                                                                                                                                                                                                                                                                                                                            | ✅ DONE | 2026-05-06 |
| 9c      | 4.8a   | Backend `/inventory/items` envelope rebuild — broken-by-shape + NOVEL `customValuesByItem` sibling map (§D17). Denormalised per-row `customValues`. unit 254/254, api 64/64.                                                                                                                                                                                                                                                                                                                                                                                                                                              | ✅ DONE | 2026-05-06 |
| 9d      | 4.8b   | FE `inventory/lists/[id]` URL-state — `apiFetchPaginatedWithPermission<InventoryItemWithCustomValues>` (§D6); per-row `customValues` (§D17); legacy `ItemsPage` + `customValuesByItem` deleted. svelte 0/0.                                                                                                                                                                                                                                                                                                                                                                                                               | ✅ DONE | 2026-05-06 |
| 10a     | 4.9a   | Backend `/documents` envelope rebuild — broken-by-wrapper-key drift (§D9 / §D18). Renamed `documents` → `items`; bundled server-side `?sort` dispatch (4 cases). Cross-module touch: blackboard-attachments + chat.controller updated. unit 82/82, api 6/6.                                                                                                                                                                                                                                                                                                                                                               | ✅ DONE | 2026-05-06 |
| 10b     | 4.9b   | FE `documents-explorer` URL-state — `apiFetchPaginatedWithPermission<RawDocument>` (§D6); URL contract `?page`/`?search`/`?accessScope`/`?sort`/`?conversationId` (verbatim backend names, §D5). Pagination block ADDED (W1). Sort wired to URL (W2). Folder-sidebar counts dropped (W3 / Known Limit. #11). `state-data.svelte.ts` + `filters.ts` emptied per §D19. svelte 0/0, ESLint clean.                                                                                                                                                                                                                            | ✅ DONE | 2026-05-06 |
| 10c     | 4.10a  | Backend `/surveys` envelope rebuild — broken-by-shape (§D15 mirror); 3 count helpers + parallel `Promise.all`; unit 245/245, api 12/12.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | ✅ DONE | 2026-05-06 |
| 10d     | 4.10b  | FE `manage-surveys` URL-state — `apiFetchPaginatedWithPermission<Survey>` (§D6) × four parallel section calls. New §D20 (Beendete merger). URL contract `?draftPage`/`?activePage`/`?completedPage` + shared `?search` — explicit §D5 exception (FE-internal structural pivot, no value aliasing). Search-change resets all three section pages to 1. `_lib/api.ts` trimmed (5 dead loaders + `createFromTemplate` dropped); `_lib/handlers.ts` trimmed (`reloadSurveys` + non-Invalidate `saveSurvey`/`handleDeleteSurvey` dropped); `_lib/types.ts` trimmed (`PaginatedResponse<T>` removed). svelte 0/0, ESLint clean. | ✅ DONE | 2026-05-06 |
| 11a     | 4.11a  | Backend `/tpm/plans` + `/tpm/cards` envelope rebuild — §D21 §D15 mirror.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | ✅ DONE | 2026-05-06 |
| 11b     | 4.11b  | FE TPM URL-state migration                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |         |            |
| 12      | 4.12a  | **EXPANDED** `PickerTypeahead.svelte` component + unit tests                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |         |            |
| 13      | 4.12b  | **EXPANDED** Wire all 6 picker sites (5 Lead + 1 Employee) to component + smoke tests                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |         |            |
| 14      | 5.1    | API integration tests across all migrated endpoints                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |         |            |
| 15      | 5.2-4  | ADR + HOW-TO update + `?limit=100` Phase-1 hack cleanup                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |         |            |

### Session log template

```markdown
### Session {N} — {YYYY-MM-DD}

**Goal:** {what should be achieved}
**Result:** {what was actually achieved}
**New files:** {list}
**Changed files:** {list}
**Verification:**

- ESLint: {0 errors / N errors → fixed}
- Type-check: {0 errors}
- Tests: {N / N passed}
- Manual smoke: {pass / fail per per-page DoD}

**Deviations:** {what differed from plan and why}
**Next session:** {what comes next}
```

---

## Spec Deviations

| #   | Spec says                                                                                                                                                                                                                                                                                                        | Actual code                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | Decision                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| --- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| D1  | §0.1 records branch `feat/server-driven-pagination` checked-out (✅ at sign-off)                                                                                                                                                                                                                                 | Git working branch is `fix/pagination` — Stage A and Stage B both landed here                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | Accepted. No risk: branch name is metadata, content matches plan. Future PR title can still use the canonical `feat/server-driven-pagination` for review surface.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| D2  | §1.2a-B B-3 SQL ILIKE target listed as "p.title (oder name)"                                                                                                                                                                                                                                                     | TPM plan column is `name` (verified via `tpm_maintenance_plans` schema + the existing `ORDER BY p.name` in `tpm-plans.service.ts:262`). ILIKE applied to `p.name` only.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | Plan ambiguity resolved at implementation time. No description column on tpm_maintenance_plans, so single-column ILIKE is correct.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| D3  | §1.2a-B implies one helper change per service method                                                                                                                                                                                                                                                             | B-3 `tpm-plans.service.ts` factored out a new module-level pure helper `buildListPlansClauses(startIdx, teamIds, search)` to keep the COUNT and SELECT branches DRY and stay within the 60-line max-lines-per-function budget                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | Justified by ESLint `max-lines-per-function` rule + KISS. Helper is colocated with the service, not exported.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| D4  | §3.1 step 3 says "check existing pattern in another paginated endpoint, e.g. `/users` paginated query"                                                                                                                                                                                                           | `/users` (`backend/src/nest/users/users.service.ts:159-168`) emits **non-canonical** keys: `{ data, pagination: { currentPage, totalPages, pageSize, totalItems } }`. The FE `apiFetchPaginated` type-guard (`frontend/src/lib/server/api-fetch.ts:217 isCompletePagination`) requires `{ page, limit, total, totalPages }` — `/users` would fail it cleanly. ADR-007 §"pagination" + the FE-helper contract (Phase-2 §2.1, masterplan changelog 1.4.0) are the binding spec.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | Followed ADR-007 + FE helper, NOT `/users`. `dummy-users` is the new canonical reference — `/users` itself becomes a Phase-4.1/4.2 migration target (`manage-employees`/`manage-admins`). The `/users` migration will rename `currentPage→page`, `pageSize→limit`, `totalItems→total` and the FE-helper will then accept it. **✅ Resolved by Step 4.1a (2026-05-04, changelog 1.10.0):** rename landed atomically; `users.service.ts` now emits the canonical envelope; `apiFetchPaginated<SafeUserResponse>` type-guard now accepts `/users`. Phase 4.1b is unblocked.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| D5  | §3.2 enumerates only `?page` + `?search` URL params (boxes 4 + 6); does not mention status filter                                                                                                                                                                                                                | Status filter HAD to migrate to URL too. Counter-factual: leaving `statusFilter` as client `$state` while migrating `?page`/`?search` to URL would create a broken pattern — clicking a pagination `<a href={pageHref(2)}>` link runs server load with NO `isActive`, dropping the filter the user just selected. The pagination would silently lose the filter on every page change. URL convention used: `?isActive=N` (numeric IS_ACTIVE codes 0/1/3, matches backend param verbatim — no FE-side aliasing layer).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | Justified by correctness, not scope creep. Phase 4 per-page DoD §4 explicitly says "Filter (status / role / etc.) re-fetches and resets to page 1" — the reference impl must demonstrate this pattern so Phase-4 pages copy it, otherwise every Phase-4 page has to re-derive the same fix individually. Recorded for transparency.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| D7  | §4.2 lists FE migration scope as `+page.server.ts` + `+page.svelte` + `_lib/utils.ts` + `_lib/types.ts` (drop `PaginationPageItem`) + `_lib/filters.ts` (gut) + `_lib/SearchResults.svelte` (gut). Does not enumerate edits to the `Admin` interface in `_lib/types.ts` beyond the `PaginationPageItem` removal. | Added a single optional field `role?: 'admin' \| 'root' \| 'employee'` to the `Admin` interface (`_lib/types.ts`). Required because the page-server defensive role filter (`adminsResult.data.filter((u: Admin) => u.role === 'admin')`) — itself a documented §0.2 R2 mitigation pattern from manage-employees Phase 4.1b — won't compile against an interface that doesn't model the field. The `role` value IS naturally present on `/users` JSON responses; the previous interface just didn't model it.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Justified by correctness, not scope creep. The defensive filter is a §4.1b reference pattern that Phase-4 pages copy verbatim; without modelling `role` on the entity type, the pattern can't be applied without a cast. Optional field (`?`) chosen to avoid breaking `populateFormFromAdmin` and other consumers that construct `Admin` partials. JSDoc on the field explains the drift-defence purpose. Recorded for transparency. Same edit will likely recur in Phase 4.5+ pages (`Suggestion`, `BlackboardEntry`, etc. probably have the same mismatch).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| D6  | §4.1b lists FE migration scope as `+page.server.ts` + `+page.svelte` + `_lib/utils.ts` only — no Phase-2 helper extension                                                                                                                                                                                        | `manage-employees` (and every Phase 4.3-4.11 addon-gated page after it) renders `<PermissionDenied />` on backend 403 (ADR-020 §"Frontend Permission-Denied Handling"). The base `apiFetchPaginated<T>` helper deliberately collapses 403 into the empty-result envelope (Phase-2 sign-off, `api-fetch.test.ts:475-485` documents this). Without a permission-aware paginated variant, every addon-gated migration would either (a) lose the `<PermissionDenied />` UX (regression), (b) probe-hack with two parallel fetches, or (c) extend the base helper and break its 50 shape-asserting tests.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Added new helper `apiFetchPaginatedWithPermission<T>` in Step 4.1b alongside the consumer migration. Purely additive (R7 preserved): existing `apiFetchPaginated` and its 50 tests are unchanged; the new helper adds 14 dedicated tests in the same file. The Phase-2 test-comment at line 475-485 explicitly named this helper as the future composition point ("paginated callers that need permission-aware UI compose apiFetchPaginated with apiFetchWithPermission, **or a future apiFetchPaginatedWithPermission**") — this is the missing piece of the Phase-2 helper layer, not a divergence from the design intent. Recorded so Sessions 7-11 know which helper to use (`apiFetchPaginatedWithPermission` for addon-gated; `apiFetchPaginated` for `(root)`-only).                                                                                                                                                                                                                                                                                                                                                                                                |
| D8  | §Migration order line 297 + Sessions table row 7c said "Standard Phase-4 migration" for `manage-assets` (i.e. expected backend already returned canonical envelope, FE migration only)                                                                                                                           | Pre-flight audit (Session 7c, 2026-05-05) found `assets.service.ts:listAssets()` returned plain `Promise<AssetResponse[]>` with no LIMIT/OFFSET, no COUNT, no envelope. Controller never passed `query.page`/`query.limit` through despite the DTO already extending `PaginationSchema` (Stage 1.2a-A, 2026-05-01). The FE `?limit=100` band-aid was silently dropped. Worse than 4.3 broken-by-mirror (which had a wrong envelope to rename — here there was no envelope at all).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | Sub-split per §D4 dependency-chain precedent (mirrors 4.1a/4.1b and 4.3a/4.3b): 4.4a backend rebuild this session (Session 7c, ✅ DONE 2026-05-05, changelog 1.14.0) + 4.4b FE URL-state migration next session (Session 7d, PENDING). Plan-text reclassification recorded in Migration order row 4.4 + Sessions table row 7c (closed) + new row 7d (pending). Recorded for transparency — same per-page atomicity precedent the masterplan describes (line 12). The R2 risk register row already mandated this audit pattern; Session 7c executed it correctly. Lesson for future Phase-4 pages: do NOT trust pre-Phase-2 plan notes about backend pagination state — Stage 1.2a-A only fixed the request-side DTO; service-side wiring varies per endpoint. Sessions 8-11 should audit before declaring "Standard Phase-4 migration".                                                                                                                                                                                                                                                                                                                                     |
| D9  | §Migration order line 301 + Sessions table row 8 said "KVP + blackboard" as a single session implying standard FE-only migration for KVP                                                                                                                                                                         | Pre-flight audit (Session 8a, 2026-05-05, executed per §D8 lesson) found `kvp.service.ts:listSuggestions()` returned `{ suggestions, pagination: { currentPage, pageSize, totalItems, totalPages } }`. **Two compounded drifts**: (a) wrapper key `suggestions` is not in `ResponseInterceptor.isPaginatedResponse`'s recognised set (`items`/`entries`/`data`, response.interceptor.ts:65) → interceptor passed the object through naively → **no `meta.pagination` was ever emitted** (FE today reads `data.suggestions` via legacy `parseSuggestionsResponse` workaround, line 31-37 of `+page.server.ts`); (b) pagination keys drifted to `currentPage/pageSize/totalItems` (same shape `/users` had pre-§D4 / Step 4.1a). Worse than `/users` because the wrapper-key drift hides the entire `meta.pagination` block, not just renames keys. Bonus discovery: the new canonical envelope test surfaced a pre-existing pg-bigint→string drift in `executeCountQuery` (kvp.service.ts) — `total` was a string at runtime despite the `Promise<number>` type lie. Fixed at source by mirroring the existing line-250 coercion pattern (`Number(countRows[0]?.total ?? 0)`). | Sub-split per §D4/§D8 dependency-chain precedent: 4.5a backend envelope rename + bigint coercion ✅ DONE Session 8a 2026-05-05 (changelog 1.16.0); 4.5b FE URL-state migration ✅ DONE Session 8b 2026-05-06 (changelog 1.17.0). User-decisions captured in pre-execution AskUserQuestion (Q1 sub-split=Y, Q2 BadgeCounts=Option C defer, Q3 drop FE-only filters in §D9, Q4 start 4.5a now). FE-only filter values dropped in 4.5b: `assetFilter` (no backend `assetId` filter), `currentFilter='asset'` (backend `OrgLevelSchema` has no `'asset'` member, only `company/department/area/team`), `currentFilter='manage'` (no backend equivalent). BadgeCounts in `state-filters.svelte.ts:65` removed in 4.5b — they were already showing meaningless numbers post-Phase-1 (counted only loaded subset, not full result set). Lesson reaffirmed (extends §D8 lesson): even when the backend HAS pagination + envelope, audit BOTH the wrapper key AND the pagination key names against `response.interceptor.ts:65 isPaginatedResponse` BEFORE declaring "Standard Phase-4 migration" — wrapper-key drift is a silent data-shape bug that the type-checker cannot catch. |
| D10 | §Migration order row 4.5b + §D9 enumerated only `assetFilter` / `currentFilter='asset'` / `currentFilter='manage'` as the FE-only filter values to drop. The `departmentFilter` dropdown (mapped to `?departmentId=` in pre-Phase-4.5b `api.ts:148`) was not on the list.                                        | `ListSuggestionsQuerySchema` (`backend/src/nest/kvp/dto/query-suggestion.dto.ts`) does not declare `departmentId`. Zod default behaviour strips unrecognised keys → the `?departmentId=N` query param has been a silent no-op the entire time. Keeping the dropdown post-Phase-4.5b would either (a) leave it as client `$state` while every sibling filter migrates to URL — exact violation of the §D5 rule that drove the original masterplan addition (clicking page-2 link silently drops a non-URL filter), or (b) wire it to a URL param the backend ignores — same dishonest UI. Backend extension to add `departmentId` is out of 4.5b scope.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | Dropdown removed in Session 8b alongside the §D9-listed values. Users wanting "department-level suggestions" use the existing `?orgLevel=department` view-toggle (semantically broader: shows all dept-level suggestions, not a single dept). A specific-department filter is V2 follow-up under §Known Limitations: requires backend `ListSuggestionsQuerySchema.departmentId` extension + matching ILIKE / equality clause in `kvp.helpers.ts:buildFilterConditions` + new URL contract entry. Recorded for transparency: the masterplan's §D9 list was non-exhaustive, but the §D5 rule's logical extension forced this drop atomically.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| D11 | §4.5b Migration order text + §D9 sub-split did not enumerate the `'restored'` value of the FE status dropdown (`STATUS_FILTER_OPTIONS` in `_lib/constants.ts:174`).                                                                                                                                              | Backend `StatusSchema` (`backend/src/nest/kvp/dto/query-suggestion.dto.ts:14`) accepts only `['new', 'in_review', 'approved', 'implemented', 'rejected', 'archived']` — selecting "Wiederhergestellt" in the dropdown produced a silent backend-400 (Zod validation rejected the unknown enum value, controller returned validation error, FE rendered an empty list with no user feedback). Same dishonest-UI category as §D10.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Status option `'restored'` removed from `STATUS_FILTER_OPTIONS` in Session 8b. URL allow-list mirrors the backend enum verbatim (§D5 — no FE-side aliasing). Lesson reaffirmed: when migrating any FE filter dropdown to a URL contract, audit each dropdown option against the backend enum BEFORE building the allow-list. Recorded for transparency.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| D12 | §4.6 did not enumerate the FE-only `status: 'active'` URL param (`+page.server.ts:73` pre-Phase-4.6).                                                                                                                                                                                                            | Backend `ListEntriesQuerySchema` has no `status` field → Zod-stripped silently. Backend default `isActive ?? 1` covered active-only by coincidence. Same dishonest-UI category as §D10/§D11.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Param dropped in Session 8c. FE has no UI for non-active status (only search/level/sort in `BlackboardFilterCard`); `/blackboard/archived` covers archived view via own hardcoded `isActive=3`. Lesson reaffirmed: audit every FE-injected URL param against backend DTO BEFORE migration.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| D13 | §4.6 row 4.6 + Sessions row 8c only enumerated main `/blackboard`. The `/blackboard/archived` sub-route (`?limit=100` band-aid) was a separate Beta-correctness bug.                                                                                                                                             | Both routes share backend endpoint, DTO, response shape, and FE `_lib/` types. Splitting would have duplicated session-overhead.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Folded into Session 8c. Archived URL contract `?page` only (no search/filter UI by current product spec). When a route group shares a single backend endpoint across multiple list views, fold to keep the touch-set atomic.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| D14 | §4.6 sub-split named only `+page.server.ts` + `+page.svelte` rewrites. Dead `_lib/api.ts` + `_lib/types.ts` + `_lib/constants.ts` orphans were not enumerated.                                                                                                                                                   | Pre-flight `grep` confirmed `fetchEntries` + `buildQueryParams` had no external call sites; `PaginatedResponse<T>` + `FilterState` + local `PaginationMeta` consumed only by dead `fetchEntries`; `ENTRIES_PER_PAGE` consumed only by dead `buildQueryParams`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | All deleted in Session 8c (mirrors §4.5b KVP `fetchSuggestions` cleanup). `$lib/server/api-fetch.ts` `PaginationMeta` + `PaginatedPermissionResult<T>` are now single source of truth. Per-page DoD already mandates "Old client-side helpers deleted".                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| D16 | §Migration order row 4.7 + §Sessions row 9b said "ADD search input to admin view" only, and "DELETE `_lib/types.ts: PaginatedResponse<T>`" without naming non-list consumers.                                                                                                                                    | Search added on BOTH routes (URL contract on row 4.7 already named `?search` for both; employee-only with no input would have been the §D10/D11/D12 anti-pattern in reverse). `[uuid]/+page.server.ts` consumed `PaginatedResponse<WorkOrderComment>` directly via `apiFetch` (bypasses `extractPaginated`); replaced with a local `LegacyCommentsEnvelope<T>` interface — same shape as the api.ts internal `LegacyCommentsPage<T>`, scope-bounded to the detail page. Both V2 deferred per Known Limitation #10.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | Recorded for transparency. Lesson: when deleting a public type, grep ALL consumers (not just the one named in the masterplan), and treat URL-contract symmetry as UI-symmetry by default.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| D15 | §4.7 row + §Sessions row 9a said "audit-first per §D8/§D9 — could be broken-by-mirror like KVP" without enumerating drift form.                                                                                                                                                                                  | NEW drift: `work-orders.service.ts:68-73` `PaginatedWorkOrders` returned flat `{ items, total, page, pageSize }`. Wrapper key `items` IS recognised but `'pagination' in data` is the FIRST gate in `isPaginatedResponse` → no `pagination` block ⇒ `meta.pagination` never emitted. Both `listWorkOrders` + `listMyWorkOrders` share `buildPaginatedList` (single edit covers both). `WorkOrderCommentsService.listComments` has same drift, scoped OUT → Known Limit. #10.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | 4.7a ✅ Session 9a (1.19.0); 4.7b FE atomic per §D13. **Lesson:** wrapper-key recognised + NO `pagination` block IS broken-by-shape too. Audit checklist: verify BOTH `'pagination' in result` AND wrapper-key membership AND canonical pagination-key names against `response.interceptor.ts:65` before declaring "Standard".                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| D17 | §4.8 row + §Sessions row 9c said "broken-by-shape like work-orders" without anticipating a 4th drift dimension beyond §D15's three.                                                                                                                                                                              | Same form-class as §D15 PLUS NOVEL: `inventory-items.service.ts:84 findByList()` returned `{ items, total, customValuesByItem: Record<itemId, …> }` — a sibling map at root used by FE for custom-field columns. `extractPaginatedItems` keeps only `items` → sibling silently stripped, losing per-item custom values FE-wide.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | Denormalise: in-memory `rows.map(r => ({ ...r, customValues: customValuesByItem[r.id] ?? [] }))` → `InventoryItemRowWithCustomValues[]` (extends lean `InventoryItemRow` so `create`/`update`/`findByUuid` paths stay unchanged). Single SQL query preserved. 4.8a ✅ Session 9c (1.21.0); 4.8b ✅ Session 9d. **Lesson:** §D15 checklist needs a 4th check — grep service return-types for `Record<…>` or non-pagination object keys at wrapper level before declaring scope.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| D19 | §D14 cleanup discipline (4.5b/4.6/4.7b/4.8b) `git rm`s orphan `_lib/` files after the rewrite.                                                                                                                                                                                                                   | Harness blocks `rm`/`git` (CLAUDE.md). `_lib/state-data.svelte.ts` + `_lib/filters.ts` emptied to `export {};` stubs — no production importer reaches them; svelte-check + ESLint clean.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **User follow-up:** `git rm frontend/src/routes/(app)/(shared)/documents-explorer/_lib/{state-data.svelte.ts,filters.ts}`. **Pattern when `rm` blocked:** stub + record here + flag in session report.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| D18 | §4.9 row + §Sessions row 10 said "File lists per folder" without enumerating drift form. Pre-edit assumption: standard FE-only migration. Initial backend-grep scoped to `backend/src/nest/documents/` only.                                                                                                     | Wrapper-key drift identical to KVP §D9: `documents.service.ts:240` returned `{ documents: [...], pagination: {...} }` — `documents` NOT in `isPaginatedResponse`'s recognised set ⇒ `meta.pagination` never emitted. §D8/§D15 PASS, §D17 PASS. **Three NOVEL wrinkles:** (W1) no pagination UI — backend `limit=20` silently capping; must be ADDED in 4.9b. (W2) sort was FE-only over loaded subset; once paginated, 3 of 4 options become silent no-ops (§D11 dishonest-UI). (W3) `categoryCounts` counted only loaded subset (KVP `BadgeCounts` Option-C class). Cross-module callers caught by type-check: `blackboard-attachments.service.ts:83` + `chat.controller.ts:442`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | 4.9a ✅ Session 10a (1.23.0) — wrapper rename + ADR-007 envelope + bundled `?sort` via `buildDocumentsOrderByClause` (tiebreaker `d.id ASC`); 4.9b ✅ Session 10b (1.24.0). W2: backend extension chosen over UI drop (preserves Beta UX, ~6 LOC). W3 deferred → Known Limit. #11. Chat controller's external `{ documents, total }` shape intentionally preserved. **Lessons:** (a) file-explorer pages have UX wrinkles backend-only audit can't see — check rendered FE for client-side sort/filter, count badges, sub-navigation; (b) when renaming return-shape fields, grep ENTIRE `backend/src/nest/` not just the owning module.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |

| D20 | §4.10b "two parallel calls for `completed`+`archived`" did not specify URL surface or merged-pagination math. | Single `?completedPage=N` URL key drives TWO parallel `apiFetchPaginatedWithPermission<Survey>` calls (`status=completed` + `status=archived`) at same page index; results concat (completed-then-archived); merged pagination = `total` summed, `totalPages = max(...)`, `page`/`limit` shared. Trade-off: items-per-page varies when sub-totals differ — honest under §D11 (FE-side re-pagination over a 2L window would re-introduce the §0.2.1-B1 truncation class). | Single key + `mergeBeendetePagination` helper. **Lesson:** for visual sections folding N≥2 backend statuses, prefer one URL pivot + parallel calls + `max(totalPages)` over FE-side re-pagination (re-introduces truncation) or N independent pivots (UX clutter). |
| D21 | §4.11 row + §Sessions row 11 said "Two endpoints, can do in one session". DTOs already canonical (Phase 1.2a-B B-3/B-4) ⇒ assumed Standard FE-only. | Pre-flight audit (Session 11a, executed per §D8 lesson) found pure §D15 mirror on BOTH services: `PaginatedPlans` (`tpm-plans.service.ts:47-52`) + `PaginatedCards` (line 60-65) returned `{data, total, page, pageSize}` — wrapper key `data` recognised but no `pagination` block ⇒ `meta.pagination` never emitted. No §D17 sibling-map. No §D9 bigint (`Number.parseInt` since first commit). **Cross-module:** `tpm-plans.controller.ts:467` (`GET /tpm/plans/:uuid/board`) imports + returns `PaginatedCards` — single shape change covers it. `PaginatedDefects` + `PaginatedExecutions` (in `tpm-executions.service.ts`) share same drift class but scoped OUT → Known Limit. #12. | Sub-split 4.11a (1.27.0, Session 11a) + 4.11b. Single edit per service: plans `listPlans` return; cards `executePaginatedQuery` (covers `listCardsForAsset` + `listCardsForPlan` + `getCardsByStatus` + cross-module `:uuid/board`). Service-internal `pageSize` parameter kept (no signature churn) — maps to canonical `limit` in return-shape only. **Lesson:** when one helper feeds N callers AND a cross-controller imports the shape, single edit at the helper covers all surfaces — grep `Promise<PaginatedX>` before edit confirms scope. (extends §D15) |

---

## Known Limitations (V1 — deliberately excluded)

1. **`manage-halls` / `manage-areas` / `manage-departments` / `manage-teams`** — backend does not paginate these endpoints (no `PaginationSchema` extension). Out of scope for V1. Beta tenants are not expected to have >50 of any of these (typical tenant: ~5–20 areas, ~10–50 departments, ~20–100 teams). Re-evaluate post-Beta if real data shows otherwise. Mitigation if needed: client-side pagination on the rendered list (no backend touch).
2. **Lead-picker modal dropdowns** (5 endpoints — get `&limit=100` in Phase 1.3, then stay at that ceiling):
   - `manage-areas` → `/users?role=admin&isActive=1&position=area_lead`
   - `manage-areas` → `/users?role=root&isActive=1&position=area_lead`
   - `manage-departments` → `/users?role=admin&isActive=1&position=department_lead`
   - `manage-departments` → `/users?role=root&isActive=1&position=department_lead`
   - `manage-teams` → `/users?isActive=1&position=team_lead`

   Realistic tenant volume: 1 root, 5–20 area-leads, 5–20 department-leads, 10–50 team-leads. The 100 ceiling is structurally unreachable for the Beta target audience. All 5 endpoints already filter `isActive=1` server-side, so inactive leads cannot push active ones out of the result. If a tenant ever lands a 101st active lead candidate the dropdown silently truncates — risk accepted. Migration path if needed: same typeahead pattern as Phase 4.12. **Note:** the employee-picker (`/users?role=employee` in the team-member assignment) is NOT a Known Limitation — it IS migrated in Phase 4.12 because (a) tenant volume realistically exceeds 100 employees and (b) it currently lacks `isActive=1` (audit B3 in §0.2.1).

3. **Cursor / keyset pagination** — current backend uses `page` + `limit` (offset). Acceptable up to ~10 k records per tenant per type. If a single tenant grows past that, a dedicated optimization plan is needed (would require backend rewrite of every list endpoint).
4. **Saved searches / persistent filter presets** — out of scope. URL is the only state.
5. **Real-time list updates via SSE** — out of scope. Lists refresh on mutation via `invalidateAll()`.
6. **Bulk actions across pages** — out of scope. Bulk-select stays per-page.
7. **Chat history pagination** — already handled by WebSocket `?before=<msgId>` cursor pattern, not affected.
8. **Calendar / shift views** — date-bound rendering, not list-bound. Not in scope.
9. **KVP — specific-department filter** (§D10, recorded 2026-05-06): the pre-Phase-4.5b `departmentFilter` dropdown on `/kvp` was a silent backend no-op (`?departmentId=` was Zod-stripped because `ListSuggestionsQuerySchema` doesn't declare it). Dropped in Session 8b. The `?orgLevel=department` view-toggle still filters to "all department-level suggestions" (semantically broader). **V2 follow-up if real beta usage demands single-department filtering:** add `departmentId: IdSchema.optional()` to `ListSuggestionsQuerySchema`, extend `kvp.helpers.ts:buildFilterConditions` with `s.department_id = $N` clause, add `?departmentId=` to the FE URL contract + bring the dropdown back. Estimate: 1 backend session + 1 FE session.
10. **Work-orders — comments pagination envelope** (§D15, recorded 2026-05-06): `WorkOrderCommentsService.listComments` returns the same flat `{ items, total, page, pageSize }` shape as the pre-Phase-4.7a list endpoints — wrapper key `items` recognised but `pagination` block absent, so `ResponseInterceptor.isPaginatedResponse` returns `false` and `meta.pagination` is never emitted (same drift class as work-orders list, audit found in Session 9a). FE consumes via `_lib/api.ts:211 fetchComments` + `extractPaginated` workaround. Scoped OUT of 4.7a: comments per work-order rarely exceed 20 in practice, NOT a Beta-correctness blocker, and atomic inclusion would inflate Session 9a beyond the masterplan's session-budget discipline. **V2 follow-up:** rename `listComments` return to canonical `{ items, pagination: { page, limit, total, totalPages } }`, update `work-orders.controller.ts:317 listComments` return type forwarding (already uses `ReturnType<...>`, no controller-edit needed if signature stays the same), update the comments-listing api integration test (currently `backend/test/work-orders.api.test.ts:411-426`), update FE `_lib/api.ts:211 fetchComments` to consume `apiFetchPaginated`. Estimate: ~½ session backend + ½ session FE — small enough to bundle with another V2 cleanup or fold into Phase 5.
11. **Documents — folder-sidebar count badges** (§D18 W3, recorded 2026-05-06): pre-Phase-4.9 the folder sidebar in `documents-explorer` rendered "Personal: N / Team: N / Department: N / …" badges derived client-side from the loaded document subset (`_lib/state-data.svelte.ts:59 calculateCategoryCounts(allDocuments)`). With backend default `limit=20`, those numbers were already silently wrong on any tenant with >20 documents per scope — counted only the loaded page, not the full result set. Same dishonest-UI class as KVP `BadgeCounts` (§D9 Q2 → Option C deferred). Counts dropped in Session 10b — total document count remains visible in the page-header `stats` block. **V2 follow-up if real Beta usage demands per-scope counts:** add `/documents/counts-by-scope` endpoint returning `{ scope, count }` rows from `SELECT access_scope, COUNT(*) FROM documents WHERE tenant_id=$1 AND is_active=$2 GROUP BY access_scope` (RLS-scoped via `tenantQuery`), expose in `(app)/(shared)/documents-explorer/+page.server.ts` via `Promise.all([documents, counts])`, render badges in `_lib/FolderSidebar.svelte`. Estimate: ~½ session backend + ~½ session FE.
12. **TPM defects + executions pagination envelope** (§D21, recorded 2026-05-06): `tpm-executions.service.ts` exports `PaginatedDefects` + `PaginatedExecutions` with the same flat `{data, total, page, pageSize}` shape as `PaginatedPlans`/`PaginatedCards` before 4.11a (pure §D15 mirror). Three controller routes affected: `GET /tpm/cards/:uuid/executions`, `GET /tpm/cards/:uuid/defects`, `GET /tpm/plans/:uuid/defects`. Existing api test `tpm-plans.api.test.ts:835 TPM: Plan Defects — Empty Plan` still asserts the legacy shape. Scoped OUT of 4.11a because masterplan §4.11 names only `/tpm/plans` + `/tpm/cards`; lists rarely exceed 50 records ⇒ NOT a Beta-correctness blocker; atomic inclusion would inflate Session 11a beyond session-budget discipline. **V2 follow-up:** rename `PaginatedDefects` + `PaginatedExecutions` to canonical envelope `{ items, pagination: { page, limit, total, totalPages } }` in `tpm-executions.service.ts`, update the 3 consuming controller routes (return-type forwarding via `ReturnType<...>` if signatures stay identical), update the affected api test. Estimate: ~½ session backend + ~½ session test sweep — fold into Phase 5.

---

## Post-Mortem (fill after completion)

### What went well

- (empty)

### What went badly

- (empty)

### Metrics

| Metric                   | Planned | Actual           |
| ------------------------ | ------- | ---------------- |
| Sessions                 | 17      | 1 + 2a + 2b + 2c |
| Backend files changed    | ~12     | —                |
| New backend files        | 0       | —                |
| New frontend files       | 3       | —                |
| Frontend files changed   | ~28     | —                |
| Migration files          | 0       | —                |
| Unit tests               | ~12     | —                |
| API tests                | ~32     | —                |
| ESLint errors at release | 0       | —                |
| Spec deviations          | 0       | —                |

---

**This document is the execution plan. Every session starts here, takes the next unchecked item, and marks it done. No coding starts before §0 is signed off.**
